<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CreativeAI Agent</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background: #0a0a0f; color: #e0e0e0; overflow: hidden; height: 100vh; }
        h1, h2, h3, h4 { font-family: 'Space Grotesk', sans-serif; }
        
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: #555; }

        .glass { background: rgba(255,255,255,0.03); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.06); }
        .glass-strong { background: rgba(255,255,255,0.06); backdrop-filter: blur(30px); border: 1px solid rgba(255,255,255,0.1); }

        .glow-purple { box-shadow: 0 0 30px rgba(139,92,246,0.15); }
        .glow-blue { box-shadow: 0 0 30px rgba(59,130,246,0.15); }
        
        .gradient-text { background: linear-gradient(135deg, #8b5cf6, #06b6d4, #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-size: 200% 200%; animation: gradientShift 4s ease infinite; }
        
        @keyframes gradientShift { 0%,100% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } }
        @keyframes fadeInUp { from { opacity:0; transform: translateY(15px); } to { opacity:1; transform: translateY(0); } }
        @keyframes fadeIn { from { opacity:0; } to { opacity:1; } }
        @keyframes slideInRight { from { opacity:0; transform: translateX(20px); } to { opacity:1; transform: translateX(0); } }
        @keyframes slideInLeft { from { opacity:0; transform: translateX(-20px); } to { opacity:1; transform: translateX(0); } }
        @keyframes pulse { 0%,100% { opacity:.4; } 50% { opacity:1; } }
        @keyframes spin { to { transform: rotate(360deg); } }
        @keyframes bounce { 0%,60%,100% { transform: translateY(0); } 30% { transform: translateY(-4px); } }

        .animate-fade-in-up { animation: fadeInUp .4s ease; }
        .animate-fade-in { animation: fadeIn .3s ease; }
        .animate-slide-right { animation: slideInRight .3s ease; }
        .animate-slide-left { animation: slideInLeft .3s ease; }

        .typing-dot { width: 6px; height: 6px; border-radius: 50%; background: #8b5cf6; animation: bounce 1.4s ease infinite; }
        .typing-dot:nth-child(2) { animation-delay: .2s; }
        .typing-dot:nth-child(3) { animation-delay: .4s; }

        .chat-area { height: calc(100vh - 220px); overflow-y: auto; }
        .msg-user { background: linear-gradient(135deg, rgba(139,92,246,0.2), rgba(59,130,246,0.15)); border: 1px solid rgba(139,92,246,0.2); }
        .msg-agent { background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.06); }

        .template-card { transition: all .25s ease; cursor: pointer; }
        .template-card:hover { transform: translateY(-3px); border-color: rgba(139,92,246,0.4); box-shadow: 0 8px 25px rgba(139,92,246,0.1); }

        .btn-primary { background: linear-gradient(135deg, #8b5cf6, #7c3aed); transition: all .2s; }
        .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(139,92,246,0.3); }
        .btn-secondary { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); transition: all .2s; }
        .btn-secondary:hover { background: rgba(255,255,255,0.1); }

        .sidebar { width: 320px; min-width: 320px; transition: all .3s ease; }
        .sidebar.collapsed { width: 0; min-width: 0; overflow: hidden; padding: 0 !important; border: none !important; }

        .gallery-img { transition: all .25s ease; cursor: pointer; }
        .gallery-img:hover { transform: scale(1.03); box-shadow: 0 8px 25px rgba(0,0,0,0.4); }

        .input-area { background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.08); transition: border-color .2s; }
        .input-area:focus-within { border-color: rgba(139,92,246,0.4); }

        .tab-active { background: rgba(139,92,246,0.2); color: #c4b5fd; border-color: rgba(139,92,246,0.4); }
        .tab-inactive { color: #888; border-color: transparent; }
        .tab-inactive:hover { color: #bbb; background: rgba(255,255,255,0.04); }

        .modal-overlay { background: rgba(0,0,0,0.7); backdrop-filter: blur(8px); }
        .modal-box { background: #12121a; border: 1px solid rgba(255,255,255,0.08); max-height: 90vh; overflow-y: auto; }

        .quick-action { transition: all .2s; }
        .quick-action:hover { transform: scale(1.05); background: rgba(139,92,246,0.15); }

        .slider-purple::-webkit-slider-thumb { -webkit-appearance: none; width: 16px; height: 16px; border-radius: 50%; background: #8b5cf6; cursor: pointer; }
        .slider-purple::-webkit-slider-runnable-track { height: 4px; background: rgba(139,92,246,0.3); border-radius: 2px; }
        .slider-purple { -webkit-appearance: none; width: 100%; background: transparent; }

        select { background-color: rgba(255,255,255,0.06); color: #e0e0e0; }
        select option { background: #1a1a2e; color: #e0e0e0; }

        @media (max-width: 768px) {
            .sidebar { position: fixed; left: 0; top: 0; height: 100vh; z-index: 50; width: 300px; }
            .sidebar.collapsed { width: 0; }
            .chat-area { height: calc(100vh - 180px); }
        }

        .img-generated { border-radius: 12px; max-width: 100%; max-height: 350px; object-fit: cover; }
        .upload-zone { border: 2px dashed rgba(139,92,246,0.3); transition: all .2s; }
        .upload-zone:hover, .upload-zone.dragover { border-color: rgba(139,92,246,0.6); background: rgba(139,92,246,0.05); }

        .status-online { width: 8px; height: 8px; background: #22c55e; border-radius: 50%; animation: pulse 2s ease infinite; }
    </style>
</head>
<body>
    <div class="flex h-screen">
        <!-- Sidebar -->
        <aside id="sidebar" class="sidebar glass-strong flex flex-col border-r border-white/5 overflow-hidden">
            <!-- Sidebar Header -->
            <div class="p-4 border-b border-white/5">
                <div class="flex items-center justify-between">
                    <div class="flex items-center gap-2">
                        <div class="w-9 h-9 rounded-xl bg-gradient-to-br from-violet-500 to-cyan-500 flex items-center justify-center text-white font-bold text-sm">AI</div>
                        <div>
                            <h1 class="text-sm font-bold gradient-text">CreativeAI</h1>
                            <div class="flex items-center gap-1.5"><div class="status-online"></div><span class="text-[10px] text-green-400">Online</span></div>
                        </div>
                    </div>
                    <button onclick="toggleSidebar()" class="text-gray-500 hover:text-white transition p-1 md:hidden">✕</button>
                </div>
            </div>

            <!-- Sidebar Tabs -->
            <div class="flex border-b border-white/5">
                <button onclick="switchSidebarTab('templates')" id="stab-templates" class="flex-1 py-2.5 text-xs font-medium border-b-2 tab-active">🎨 Templates</button>
                <button onclick="switchSidebarTab('settings')" id="stab-settings" class="flex-1 py-2.5 text-xs font-medium border-b-2 tab-inactive">⚙️ Controls</button>
                <button onclick="switchSidebarTab('gallery')" id="stab-gallery" class="flex-1 py-2.5 text-xs font-medium border-b-2 tab-inactive">🖼️ Gallery</button>
            </div>

            <!-- Templates Panel -->
            <div id="panel-templates" class="flex-1 overflow-y-auto p-3 space-y-2">
                <p class="text-[10px] text-gray-500 uppercase tracking-wider font-semibold mb-2 px-1">Quick Templates</p>
                <div id="templateList" class="space-y-2"></div>
            </div>

            <!-- Settings Panel -->
            <div id="panel-settings" class="flex-1 overflow-y-auto p-4 space-y-5 hidden">
                <div>
                    <label class="text-[11px] text-gray-400 uppercase tracking-wider font-semibold">Style Preset</label>
                    <select id="stylePreset" class="w-full mt-1.5 px-3 py-2 rounded-lg text-sm border border-white/10 focus:outline-none focus:border-violet-500">
                        <option value="photorealistic">📷 Photorealistic</option>
                        <option value="cinematic">🎬 Cinematic</option>
                        <option value="artistic">🎨 Artistic</option>
                        <option value="anime">🌸 Anime</option>
                        <option value="3d-render">💎 3D Render</option>
                        <option value="watercolor">🖌️ Watercolor</option>
                        <option value="oil-painting">🖼️ Oil Painting</option>
                        <option value="cyberpunk">🌃 Cyberpunk</option>
                        <option value="fantasy">🧙 Fantasy</option>
                        <option value="minimalist">◻️ Minimalist</option>
                    </select>
                </div>
                <div>
                    <label class="text-[11px] text-gray-400 uppercase tracking-wider font-semibold">Aspect Ratio</label>
                    <div class="grid grid-cols-4 gap-1.5 mt-1.5">
                        <button onclick="setRatio('1:1','1024','1024')" class="ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium bg-violet-500/20 text-violet-300 border border-violet-500/30" data-ratio="1:1">1:1</button>
                        <button onclick="setRatio('16:9','1024','576')" class="ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium btn-secondary" data-ratio="16:9">16:9</button>
                        <button onclick="setRatio('9:16','576','1024')" class="ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium btn-secondary" data-ratio="9:16">9:16</button>
                        <button onclick="setRatio('4:3','1024','768')" class="ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium btn-secondary" data-ratio="4:3">4:3</button>
                    </div>
                </div>
                <div>
                    <div class="flex justify-between items-center">
                        <label class="text-[11px] text-gray-400 uppercase tracking-wider font-semibold">Creativity</label>
                        <span id="creativityVal" class="text-xs text-violet-400">70%</span>
                    </div>
                    <input type="range" id="creativity" min="0" max="100" value="70" class="slider-purple mt-2" oninput="document.getElementById('creativityVal').textContent=this.value+'%'">
                </div>
                <div>
                    <label class="text-[11px] text-gray-400 uppercase tracking-wider font-semibold">Negative Prompt</label>
                    <textarea id="negativePrompt" rows="2" class="w-full mt-1.5 px-3 py-2 rounded-lg text-sm bg-white/5 border border-white/10 focus:outline-none focus:border-violet-500 resize-none" placeholder="blur, low quality, distorted..."></textarea>
                </div>
                <div>
                    <label class="text-[11px] text-gray-400 uppercase tracking-wider font-semibold">Enhancement</label>
                    <div class="space-y-2 mt-2">
                        <label class="flex items-center gap-2 cursor-pointer">
                            <input type="checkbox" id="enhancePrompt" checked class="w-4 h-4 rounded accent-violet-500">
                            <span class="text-xs text-gray-300">Auto-enhance prompts</span>
                        </label>
                        <label class="flex items-center gap-2 cursor-pointer">
                            <input type="checkbox" id="hdUpscale" class="w-4 h-4 rounded accent-violet-500">
                            <span class="text-xs text-gray-300">HD upscale</span>
                        </label>
                    </div>
                </div>
                <div>
                    <button onclick="resetSettings()" class="w-full py-2 rounded-lg text-xs font-medium btn-secondary">↺ Reset to Defaults</button>
                </div>
            </div>

            <!-- Gallery Panel -->
            <div id="panel-gallery" class="flex-1 overflow-y-auto p-3 hidden">
                <div class="flex items-center justify-between mb-3">
                    <p class="text-[10px] text-gray-500 uppercase tracking-wider font-semibold">Your Creations</p>
                    <button onclick="clearGallery()" class="text-[10px] text-red-400 hover:text-red-300">Clear All</button>
                </div>
                <div id="galleryGrid" class="grid grid-cols-2 gap-2"></div>
                <div id="galleryEmpty" class="text-center py-10 text-gray-600">
                    <div class="text-3xl mb-2">🖼️</div>
                    <p class="text-xs">No images yet.<br>Start creating!</p>
                </div>
            </div>

            <!-- Deploy Button -->
            <div class="p-3 border-t border-white/5">
                <button onclick="showDeployModal()" class="w-full py-2.5 rounded-xl text-xs font-semibold btn-primary text-white flex items-center justify-center gap-2">
                    🚀 Deploy & Share
                </button>
            </div>
        </aside>

        <!-- Main Area -->
        <main class="flex-1 flex flex-col h-screen min-w-0">
            <!-- Top Bar -->
            <header class="flex items-center justify-between px-4 py-3 border-b border-white/5 glass">
                <div class="flex items-center gap-3">
                    <button onclick="toggleSidebar()" class="p-2 rounded-lg hover:bg-white/5 transition text-gray-400 hover:text-white">
                        <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 12h18M3 6h18M3 18h18"/></svg>
                    </button>
                    <div>
                        <h2 class="text-sm font-semibold text-white">Chat with CreativeAI</h2>
                        <p class="text-[10px] text-gray-500">Describe what you want to create</p>
                    </div>
                </div>
                <div class="flex items-center gap-2">
                    <!-- Quick Actions -->
                    <button onclick="sendQuickAction('enhance')" title="Enhance last image" class="quick-action p-2 rounded-lg text-gray-400 hover:text-violet-400">🪄</button>
                    <button onclick="sendQuickAction('variations')" title="Create variations" class="quick-action p-2 rounded-lg text-gray-400 hover:text-violet-400">📑</button>
                    <button onclick="clearChat()" title="New chat" class="quick-action p-2 rounded-lg text-gray-400 hover:text-red-400">🗑️</button>
                </div>
            </header>

            <!-- Chat Messages -->
            <div id="chatArea" class="chat-area flex-1 overflow-y-auto px-4 py-4 space-y-4">
                <!-- Welcome Message -->
                <div class="flex justify-center animate-fade-in-up">
                    <div class="text-center max-w-lg py-8">
                        <div class="w-16 h-16 mx-auto mb-4 rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-500 flex items-center justify-center text-2xl shadow-lg shadow-violet-500/20">🎨</div>
                        <h2 class="text-xl font-bold gradient-text mb-2">Welcome to CreativeAI</h2>
                        <p class="text-sm text-gray-400 mb-6">I'm your AI creative assistant. Tell me what you want to create — images, edits, variations — and I'll bring your vision to life.</p>
                        <div class="grid grid-cols-2 gap-2 text-left">
                            <button onclick="useStarter('Create a serene sunset over mountains with golden light')" class="p-3 rounded-xl glass text-xs text-gray-300 hover:border-violet-500/30 transition text-left">
                                🏔️ "Sunset over mountains with golden light"
                            </button>
                            <button onclick="useStarter('A cozy coffee shop interior, warm lighting, rainy window')" class="p-3 rounded-xl glass text-xs text-gray-300 hover:border-violet-500/30 transition text-left">
                                ☕ "Cozy coffee shop, rainy window"
                            </button>
                            <button onclick="useStarter('Futuristic cyberpunk city street at night with neon signs')" class="p-3 rounded-xl glass text-xs text-gray-300 hover:border-violet-500/30 transition text-left">
                                🌃 "Cyberpunk city at night with neon"
                            </button>
                            <button onclick="useStarter('Portrait of a woman with flowers in her hair, soft light')" class="p-3 rounded-xl glass text-xs text-gray-300 hover:border-violet-500/30 transition text-left">
                                🌺 "Portrait with flowers, soft light"
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Upload Preview -->
            <div id="uploadPreview" class="hidden px-4 py-2 border-t border-white/5">
                <div class="flex items-center gap-3 p-2 rounded-lg glass">
                    <img id="uploadThumb" src="" class="w-12 h-12 rounded-lg object-cover">
                    <div class="flex-1 min-w-0">
                        <p id="uploadName" class="text-xs text-gray-300 truncate"></p>
                        <p class="text-[10px] text-gray-500">Reference image attached</p>
                    </div>
                    <button onclick="removeUpload()" class="text-gray-500 hover:text-red-400 text-sm">✕</button>
                </div>
            </div>

            <!-- Input Area -->
            <div class="px-4 py-3 border-t border-white/5">
                <div class="input-area rounded-2xl px-4 py-3">
                    <div class="flex items-end gap-3">
                        <!-- File Upload -->
                        <label class="cursor-pointer p-2 rounded-lg hover:bg-white/5 transition text-gray-500 hover:text-violet-400 flex-shrink-0" title="Upload reference image">
                            <input type="file" id="fileInput" accept="image/*,video/*" class="hidden" onchange="handleUpload(event)">
                            <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M18.375 12.739l-7.693 7.693a4.5 4.5 0 01-6.364-6.364l10.94-10.94A3 3 0 1119.5 7.372L8.552 18.32m.009-.01l-.01.01m5.699-9.941l-7.81 7.81a1.5 1.5 0 002.112 2.13"/></svg>
                        </label>
                        <textarea id="userInput" rows="1" class="flex-1 bg-transparent text-sm text-white resize-none focus:outline-none placeholder-gray-600 max-h-32" placeholder="Describe what you want to create..." onkeydown="handleKey(event)" oninput="autoResize(this)"></textarea>
                        <button onclick="sendMessage()" id="sendBtn" class="p-2.5 rounded-xl btn-primary text-white flex-shrink-0 transition" title="Send">
                            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M6 12L3.269 3.126A59.768 59.768 0 0121.485 12 59.77 59.77 0 013.27 20.876L5.999 12zm0 0h7.5"/></svg>
                        </button>
                    </div>
                </div>
                <p class="text-center text-[10px] text-gray-600 mt-2">CreativeAI uses Pollinations.ai • Type "help" for commands</p>
            </div>
        </main>
    </div>

    <!-- Deploy Modal -->
    <div id="deployModal" class="fixed inset-0 z-50 hidden items-center justify-center modal-overlay p-4" onclick="if(event.target===this)closeDeployModal()">
        <div class="modal-box rounded-2xl w-full max-w-2xl animate-fade-in-up">
            <div class="p-6">
                <div class="flex items-center justify-between mb-6">
                    <h3 class="text-lg font-bold gradient-text">🚀 Deploy & Access from Anywhere</h3>
                    <button onclick="closeDeployModal()" class="text-gray-500 hover:text-white text-xl">&times;</button>
                </div>

                <!-- GitHub Pages - Primary -->
                <div class="p-5 rounded-xl border-2 border-violet-500/30 bg-violet-500/5 mb-4">
                    <div class="flex items-start gap-3">
                        <div class="text-2xl">📦</div>
                        <div class="flex-1">
                            <h4 class="font-bold text-white text-sm mb-1">GitHub Pages <span class="text-[10px] bg-violet-500/20 text-violet-300 px-2 py-0.5 rounded-full ml-1">RECOMMENDED</span></h4>
                            <p class="text-xs text-gray-400 mb-3">You already have the repo! Just follow these steps:</p>
                            <div class="space-y-3">
                                <div class="flex gap-3 items-start">
                                    <div class="w-6 h-6 rounded-full bg-violet-500/20 text-violet-300 flex items-center justify-center text-xs font-bold flex-shrink-0">1</div>
                                    <div>
                                        <p class="text-xs text-white font-medium">Go to your GitHub repo</p>
                                        <p class="text-[11px] text-gray-500">Open github.com/YOUR_USERNAME/YOUR_REPO</p>
                                    </div>
                                </div>
                                <div class="flex gap-3 items-start">
                                    <div class="w-6 h-6 rounded-full bg-violet-500/20 text-violet-300 flex items-center justify-center text-xs font-bold flex-shrink-0">2</div>
                                    <div>
                                        <p class="text-xs text-white font-medium">Click "Settings" tab</p>
                                        <p class="text-[11px] text-gray-500">Found in the top navigation bar of your repo</p>
                                    </div>
                                </div>
                                <div class="flex gap-3 items-start">
                                    <div class="w-6 h-6 rounded-full bg-violet-500/20 text-violet-300 flex items-center justify-center text-xs font-bold flex-shrink-0">3</div>
                                    <div>
                                        <p class="text-xs text-white font-medium">Click "Pages" in left sidebar</p>
                                        <p class="text-[11px] text-gray-500">Under "Code and automation" section</p>
                                    </div>
                                </div>
                                <div class="flex gap-3 items-start">
                                    <div class="w-6 h-6 rounded-full bg-violet-500/20 text-violet-300 flex items-center justify-center text-xs font-bold flex-shrink-0">4</div>
                                    <div>
                                        <p class="text-xs text-white font-medium">Under "Source", select "Deploy from a branch"</p>
                                        <p class="text-[11px] text-gray-500">Choose <b class="text-violet-300">main</b> branch → <b class="text-violet-300">/ (root)</b> → Click <b class="text-green-400">Save</b></p>
                                    </div>
                                </div>
                                <div class="flex gap-3 items-start">
                                    <div class="w-6 h-6 rounded-full bg-green-500/20 text-green-300 flex items-center justify-center text-xs font-bold flex-shrink-0">5</div>
                                    <div>
                                        <p class="text-xs text-white font-medium">Wait 1-2 minutes, then visit your live site!</p>
                                        <div class="mt-1 px-3 py-2 rounded-lg bg-black/40 border border-white/10">
                                            <code class="text-[11px] text-cyan-300">https://YOUR_USERNAME.github.io/YOUR_REPO/</code>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Other Options -->
                <p class="text-[10px] text-gray-500 uppercase tracking-wider font-semibold mb-3 px-1">Other Options</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                    <div class="p-4 rounded-xl glass">
                        <h4 class="font-semibold text-xs text-white mb-1">🔺 Netlify Drop</h4>
                        <p class="text-[11px] text-gray-400">Drag & drop at app.netlify.com/drop</p>
                    </div>
                    <div class="p-4 rounded-xl glass">
                        <h4 class="font-semibold text-xs text-white mb-1">▲ Vercel</h4>
                        <p class="text-[11px] text-gray-400">Import GitHub repo at vercel.com</p>
                    </div>
                    <div class="p-4 rounded-xl glass">
                        <h4 class="font-semibold text-xs text-white mb-1">🖥️ Local Network</h4>
                        <p class="text-[11px] text-gray-400"><code class="text-cyan-400">python -m http.server 3000</code></p>
                    </div>
                    <div class="p-4 rounded-xl glass">
                        <h4 class="font-semibold text-xs text-white mb-1">☁️ Cloudflare Pages</h4>
                        <p class="text-[11px] text-gray-400">Connect repo at pages.cloudflare.com</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Image Preview Modal -->
    <div id="previewModal" class="fixed inset-0 z-50 hidden items-center justify-center modal-overlay p-4" onclick="if(event.target===this)closePreview()">
        <div class="modal-box rounded-2xl max-w-4xl w-full animate-fade-in-up p-4">
            <div class="flex items-center justify-between mb-3">
                <h4 class="text-sm font-semibold text-white truncate" id="previewTitle">Image Preview</h4>
                <div class="flex items-center gap-2">
                    <button onclick="downloadPreviewImage()" class="px-3 py-1.5 rounded-lg text-xs btn-secondary text-gray-300">⬇ Download</button>
                    <button onclick="closePreview()" class="text-gray-500 hover:text-white text-lg">&times;</button>
                </div>
            </div>
            <img id="previewImg" src="" class="w-full rounded-xl max-h-[75vh] object-contain bg-black/50">
        </div>
    </div>

    <script>
        // ============ STATE ============
        let state = {
            messages: [],
            gallery: JSON.parse(localStorage.getItem('cai_gallery') || '[]'),
            uploadedFile: null,
            uploadedDataURL: null,
            lastPrompt: '',
            lastImageURL: '',
            generating: false,
            settings: {
                style: 'photorealistic',
                width: 1024,
                height: 1024,
                ratio: '1:1',
                creativity: 70,
                negativePrompt: '',
                enhancePrompt: true,
                hdUpscale: false
            }
        };

        // ============ TEMPLATES ============
        const templates = [
            { icon: '📸', name: 'Portrait Photography', prompt: 'Professional portrait photography, beautiful lighting, shallow depth of field, shot on Canon EOS R5, 85mm lens, studio lighting', tags: ['portrait', 'photo'] },
            { icon: '🎬', name: 'Cinematic Scene', prompt: 'Cinematic film still, dramatic lighting, anamorphic lens flare, color graded, movie scene, 35mm film grain', tags: ['cinematic', 'film'] },
            { icon: '🏞️', name: 'Nature Landscape', prompt: 'Breathtaking landscape photography, golden hour, majestic scenery, National Geographic quality, wide angle lens', tags: ['nature', 'landscape'] },
            { icon: '📦', name: 'Product Photography', prompt: 'Professional product photography, clean white background, studio lighting, commercial quality, high detail', tags: ['product', 'commercial'] },
            { icon: '🏛️', name: 'Architecture', prompt: 'Architectural photography, dramatic angles, golden ratio composition, modern design, detailed textures', tags: ['architecture', 'design'] },
            { icon: '🧙', name: 'Fantasy Art', prompt: 'Epic fantasy art, magical atmosphere, detailed illustration, mystical lighting, concept art quality', tags: ['fantasy', 'art'] },
            { icon: '🌃', name: 'Cyberpunk City', prompt: 'Cyberpunk cityscape, neon lights, rain-soaked streets, futuristic, Blade Runner style, volumetric fog', tags: ['cyberpunk', 'sci-fi'] },
            { icon: '🌸', name: 'Anime Style', prompt: 'Beautiful anime illustration, Studio Ghibli inspired, detailed background, soft colors, cel shading', tags: ['anime', 'illustration'] },
            { icon: '🍕', name: 'Food Photography', prompt: 'Professional food photography, appetizing presentation, warm lighting, shallow depth of field, gourmet plating', tags: ['food', 'photo'] },
            { icon: '🎨', name: 'Abstract Art', prompt: 'Abstract digital art, vibrant colors, fluid dynamics, creative composition, modern art gallery quality', tags: ['abstract', 'digital'] },
            { icon: '🖌️', name: 'Watercolor', prompt: 'Beautiful watercolor painting, soft washes, delicate details, traditional art, paper texture visible', tags: ['watercolor', 'painting'] },
            { icon: '🧊', name: '3D Render', prompt: 'Ultra realistic 3D render, Octane render, volumetric lighting, subsurface scattering, studio lighting, 8K', tags: ['3d', 'render'] },
        ];

        // ============ STYLE ENHANCERS ============
        const styleEnhancers = {
            'photorealistic': 'photorealistic, ultra detailed, 8K UHD, shot on Canon EOS R5, professional photography',
            'cinematic': 'cinematic, dramatic lighting, anamorphic, color graded, film grain, movie still',
            'artistic': 'artistic, expressive brushstrokes, gallery quality, fine art, masterpiece',
            'anime': 'anime style, cel shading, vibrant colors, detailed illustration, studio quality',
            '3d-render': '3D render, Octane render, volumetric lighting, ray tracing, subsurface scattering, 8K',
            'watercolor': 'watercolor painting, soft washes, delicate, traditional media, paper texture',
            'oil-painting': 'oil painting, rich textures, classical technique, canvas texture, museum quality',
            'cyberpunk': 'cyberpunk aesthetic, neon lights, futuristic, dark atmosphere, high tech low life',
            'fantasy': 'fantasy art, magical atmosphere, epic, mystical lighting, concept art',
            'minimalist': 'minimalist, clean, simple composition, negative space, modern aesthetic'
        };

        // ============ INIT ============
        function init() {
            renderTemplates();
            renderGallery();
            loadSettings();
            document.getElementById('userInput').focus();
        }

        // ============ SIDEBAR ============
        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('collapsed');
        }

        function switchSidebarTab(tab) {
            ['templates', 'settings', 'gallery'].forEach(t => {
                document.getElementById(`panel-${t}`).classList.toggle('hidden', t !== tab);
                document.getElementById(`stab-${t}`).className = t === tab
                    ? 'flex-1 py-2.5 text-xs font-medium border-b-2 tab-active'
                    : 'flex-1 py-2.5 text-xs font-medium border-b-2 tab-inactive';
            });
        }

        // ============ TEMPLATES ============
        function renderTemplates() {
            const container = document.getElementById('templateList');
            container.innerHTML = templates.map((t, i) => `
                <div class="template-card p-3 rounded-xl glass" onclick="useTemplate(${i})">
                    <div class="flex items-center gap-2.5">
                        <span class="text-lg">${t.icon}</span>
                        <div class="flex-1 min-w-0">
                            <p class="text-xs font-semibold text-white">${t.name}</p>
                            <p class="text-[10px] text-gray-500 truncate">${t.prompt.substring(0, 50)}...</p>
                        </div>
                        <span class="text-gray-600 text-xs">→</span>
                    </div>
                </div>
            `).join('');
        }

        function useTemplate(index) {
            const t = templates[index];
            const input = document.getElementById('userInput');
            input.value = t.prompt;
            autoResize(input);
            input.focus();
            addAgentMessage(`📋 Loaded **${t.name}** template. You can edit the prompt or just hit send to generate!`);
            // On mobile close sidebar
            if (window.innerWidth < 768) toggleSidebar();
        }

        function useStarter(prompt) {
            document.getElementById('userInput').value = prompt;
            sendMessage();
        }

        // ============ SETTINGS ============
        function setRatio(ratio, w, h) {
            state.settings.ratio = ratio;
            state.settings.width = parseInt(w);
            state.settings.height = parseInt(h);
            document.querySelectorAll('.ratio-btn').forEach(btn => {
                btn.className = btn.dataset.ratio === ratio
                    ? 'ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium bg-violet-500/20 text-violet-300 border border-violet-500/30'
                    : 'ratio-btn px-2 py-1.5 rounded-lg text-xs font-medium btn-secondary';
            });
        }

        function loadSettings() {
            const saved = localStorage.getItem('cai_settings');
            if (saved) {
                state.settings = { ...state.settings, ...JSON.parse(saved) };
                document.getElementById('stylePreset').value = state.settings.style;
                document.getElementById('creativity').value = state.settings.creativity;
                document.getElementById('creativityVal').textContent = state.settings.creativity + '%';
                document.getElementById('negativePrompt').value = state.settings.negativePrompt || '';
                document.getElementById('enhancePrompt').checked = state.settings.enhancePrompt;
                document.getElementById('hdUpscale').checked = state.settings.hdUpscale;
            }
        }

        function saveSettings() {
            state.settings.style = document.getElementById('stylePreset').value;
            state.settings.creativity = parseInt(document.getElementById('creativity').value);
            state.settings.negativePrompt = document.getElementById('negativePrompt').value;
            state.settings.enhancePrompt = document.getElementById('enhancePrompt').checked;
            state.settings.hdUpscale = document.getElementById('hdUpscale').checked;
            localStorage.setItem('cai_settings', JSON.stringify(state.settings));
        }

        function resetSettings() {
            state.settings = { style:'photorealistic', width:1024, height:1024, ratio:'1:1', creativity:70, negativePrompt:'', enhancePrompt:true, hdUpscale:false };
            loadSettings();
            setRatio('1:1','1024','1024');
            addAgentMessage('⚙️ Settings reset to defaults.');
        }

        // ============ FILE UPLOAD ============
        function handleUpload(e) {
            const file = e.target.files[0];
            if (!file) return;
            state.uploadedFile = file;
            const reader = new FileReader();
            reader.onload = (ev) => {
                state.uploadedDataURL = ev.target.result;
                document.getElementById('uploadThumb').src = ev.target.result;
                document.getElementById('uploadName').textContent = file.name;
                document.getElementById('uploadPreview').classList.remove('hidden');
            };
            reader.readAsDataURL(file);
        }

        function removeUpload() {
            state.uploadedFile = null;
            state.uploadedDataURL = null;
            document.getElementById('uploadPreview').classList.add('hidden');
            document.getElementById('fileInput').value = '';
        }

        // ============ CHAT FUNCTIONS ============
        function handleKey(e) {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendMessage();
            }
        }

        function autoResize(el) {
            el.style.height = 'auto';
            el.style.height = Math.min(el.scrollHeight, 128) + 'px';
        }

        function scrollToBottom() {
            const chat = document.getElementById('chatArea');
            setTimeout(() => chat.scrollTop = chat.scrollHeight, 100);
        }

        function addUserMessage(text) {
            const chat = document.getElementById('chatArea');
            const hasUpload = state.uploadedDataURL;
            const div = document.createElement('div');
            div.className = 'flex justify-end animate-slide-right';
            div.innerHTML = `
                <div class="msg-user rounded-2xl rounded-br-md px-4 py-3 max-w-md">
                    ${hasUpload ? `<img src="${state.uploadedDataURL}" class="w-full max-w-[200px] rounded-lg mb-2 opacity-90">` : ''}
                    <p class="text-sm text-white/90">${escapeHtml(text)}</p>
                    <p class="text-[9px] text-white/30 mt-1 text-right">${new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</p>
                </div>
            `;
            chat.appendChild(div);
            scrollToBottom();
        }

        function addAgentMessage(text, imageURL) {
            const chat = document.getElementById('chatArea');
            const div = document.createElement('div');
            div.className = 'flex justify-start animate-slide-left';
            const formattedText = formatMarkdown(text);
            div.innerHTML = `
                <div class="msg-agent rounded-2xl rounded-bl-md px-4 py-3 max-w-lg">
                    <div class="flex items-center gap-2 mb-2">
                        <div class="w-5 h-5 rounded-md bg-gradient-to-br from-violet-500 to-cyan-500 flex items-center justify-center text-[9px] text-white font-bold">AI</div>
                        <span class="text-[10px] text-gray-500">CreativeAI</span>
                    </div>
                    <div class="text-sm text-gray-300 leading-relaxed">${formattedText}</div>
                    ${imageURL ? `
                        <div class="mt-3 relative group">
                            <img src="${imageURL}" class="img-generated cursor-pointer" onclick="openPreview('${imageURL}', '${escapeAttr(state.lastPrompt)}')" onerror="this.parentElement.innerHTML='<p class=\\'text-xs text-red-400 p-3\\'>⚠️ Image failed to load. Try regenerating.</p>'">
                            <div class="absolute top-2 right-2 flex gap-1 opacity-0 group-hover:opacity-100 transition">
                                <button onclick="downloadImage('${imageURL}')" class="px-2 py-1 rounded-md bg-black/60 text-white text-[10px] backdrop-blur-sm hover:bg-black/80">⬇ Save</button>
                                <button onclick="regenerate()" class="px-2 py-1 rounded-md bg-black/60 text-white text-[10px] backdrop-blur-sm hover:bg-black/80">↻ Redo</button>
                            </div>
                        </div>
                    ` : ''}
                    <p class="text-[9px] text-gray-600 mt-2">${new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</p>
                </div>
            `;
            chat.appendChild(div);
            scrollToBottom();
        }

        function addTypingIndicator() {
            const chat = document.getElementById('chatArea');
            const div = document.createElement('div');
            div.id = 'typingIndicator';
            div.className = 'flex justify-start animate-fade-in';
            div.innerHTML = `
                <div class="msg-agent rounded-2xl rounded-bl-md px-4 py-3">
                    <div class="flex items-center gap-2 mb-2">
                        <div class="w-5 h-5 rounded-md bg-gradient-to-br from-violet-500 to-cyan-500 flex items-center justify-center text-[9px] text-white font-bold">AI</div>
                        <span class="text-[10px] text-gray-500">CreativeAI is thinking...</span>
                    </div>
                    <div class="flex items-center gap-1.5 py-1">
                        <div class="typing-dot"></div>
                        <div class="typing-dot"></div>
                        <div class="typing-dot"></div>
                    </div>
                </div>
            `;
            chat.appendChild(div);
            scrollToBottom();
        }

        function removeTypingIndicator() {
            const el = document.getElementById('typingIndicator');
            if (el) el.remove();
        }

        // ============ AGENT LOGIC ============
        function parseIntent(text) {
            const lower = text.toLowerCase().trim();
            if (/^(help|commands|what can you do|how to use)/i.test(lower)) return 'help';
            if (/^(hi|hello|hey|good morning|good evening|sup|yo)/i.test(lower) && lower.length < 20) return 'greet';
            if (/^(thanks|thank you|thx|awesome|great|perfect|nice|cool|love it)/i.test(lower)) return 'thanks';
            if (/^(change|modify|edit|adjust|update|make it|turn it|add|remove|more|less|brighter|darker|warmer|cooler)/i.test(lower)) return 'modify';
            if (/^(enhance|upscale|improve|sharpen|hd)/i.test(lower)) return 'enhance';
            if (/^(variation|variations|similar|another|again|redo|regenerate)/i.test(lower)) return 'variation';
            if (/^(settings|config|set style|change style)/i.test(lower)) return 'settings';
            if (/^(clear|reset|new chat|start over)/i.test(lower)) return 'clear';
            return 'generate';
        }

        async function sendMessage() {
            const input = document.getElementById('userInput');
            const text = input.value.trim();
            if (!text || state.generating) return;

            saveSettings();
            addUserMessage(text);
            input.value = '';
            input.style.height = 'auto';

            const intent = parseIntent(text);

            switch (intent) {
                case 'help':
                    setTimeout(() => {
                        addAgentMessage(`## 🤖 Here's what I can do:\n\n**Create Images:**\n• Just describe what you want\n• Example: "A magical forest at twilight"\n\n**Modify Images:**\n• "Make it warmer / darker / more vibrant"\n• "Change to anime style"\n• "Add rain to the scene"\n\n**Quick Commands:**\n• "enhance" - Improve last image\n• "variations" - Create alternatives\n• "redo" - Regenerate last image\n\n**Controls:**\n• Use ⚙️ sidebar to adjust style, ratio, creativity\n• Upload reference images with 📎\n• Browse 🎨 templates for inspiration\n\n**Tips:**\n• Be descriptive for better results\n• Mention camera, lighting, mood\n• Use negative prompts to avoid unwanted elements`);
                    }, 500);
                    break;

                case 'greet':
                    setTimeout(() => {
                        addAgentMessage(`Hey there! 👋 I'm ready to create some amazing visuals for you. Just describe what you'd like to see, or check out the **templates** in the sidebar for inspiration!\n\nWhat shall we create today?`);
                    }, 300);
                    break;

                case 'thanks':
                    setTimeout(() => {
                        const responses = [
                            "You're welcome! 😊 Ready to create more whenever you are!",
                            "Glad you like it! 🎨 Want me to create variations or something new?",
                            "Happy to help! ✨ Feel free to ask for modifications or new creations!",
                            "Awesome! 🙌 Let me know if you want to adjust anything!"
                        ];
                        addAgentMessage(responses[Math.floor(Math.random() * responses.length)]);
                    }, 300);
                    break;

                case 'modify':
                    if (!state.lastPrompt) {
                        addAgentMessage("I don't have a previous image to modify. Let's create one first! Describe what you'd like to see. 🎨");
                        break;
                    }
                    state.lastPrompt = `${state.lastPrompt}, ${text}`;
                    await generateImage(state.lastPrompt, `✏️ Modified! Here's the updated version:`);
                    break;

                case 'enhance':
                    if (!state.lastPrompt) {
                        addAgentMessage("No previous image to enhance. Create one first by describing what you want!");
                        break;
                    }
                    state.lastPrompt = `${state.lastPrompt}, ultra high quality, extremely detailed, sharp focus, professional`;
                    await generateImage(state.lastPrompt, `🪄 Enhanced! Here's the improved version with more detail and clarity:`);
                    break;

                case 'variation':
                    if (!state.lastPrompt) {
                        addAgentMessage("No previous image to create variations of. Describe what you'd like to see!");
                        break;
                    }
                    await generateImage(state.lastPrompt, `📑 Here's a new variation:`);
                    break;

                case 'settings':
                    switchSidebarTab('settings');
                    if (window.innerWidth < 768) document.getElementById('sidebar').classList.remove('collapsed');
                    addAgentMessage("⚙️ I've opened the **Controls panel** for you. Adjust style, aspect ratio, creativity, and more. Your changes will apply to the next image you generate!");
                    break;

                case 'clear':
                    clearChat();
                    break;

                case 'generate':
                default:
                    let prompt = text;
                    if (state.settings.enhancePrompt) {
                        const style = styleEnhancers[state.settings.style] || '';
                        prompt = `${text}, ${style}`;
                    }
                    if (state.settings.negativePrompt) {
                        prompt += `, NOT: ${state.settings.negativePrompt}`;
                    }
                    if (state.uploadedFile) {
                        prompt += `, reference image style`;
                    }
                    state.lastPrompt = prompt;
                    await generateImage(prompt, `✨ Here's what I created for you:`);
                    removeUpload();
                    break;
            }
        }

        async function generateImage(prompt, successMsg) {
            state.generating = true;
            addTypingIndicator();

            const seed = Math.floor(Math.random() * 999999);
            const w = state.settings.hdUpscale ? state.settings.width * 1.5 : state.settings.width;
            const h = state.settings.hdUpscale ? state.settings.height * 1.5 : state.settings.height;
            const encodedPrompt = encodeURIComponent(prompt);
            const imageURL = `https://image.pollinations.ai/prompt/${encodedPrompt}?width=${Math.round(w)}&height=${Math.round(h)}&seed=${seed}&nologo=true`;

            try {
                // Preload image
                const img = new Image();
                img.crossOrigin = 'anonymous';
                await new Promise((resolve, reject) => {
                    img.onload = resolve;
                    img.onerror = reject;
                    img.src = imageURL;
                    // Timeout after 60s
                    setTimeout(() => reject(new Error('timeout')), 60000);
                });

                removeTypingIndicator();
                state.lastImageURL = imageURL;
                addAgentMessage(successMsg, imageURL);
                addToGallery(imageURL, prompt);

            } catch (err) {
                removeTypingIndicator();
                addAgentMessage(`⚠️ Image generation took too long or failed. Let me try again with a simpler approach. You can also:\n• Try a shorter prompt\n• Click ↻ to regenerate\n• Check your internet connection`);
            }
            state.generating = false;
        }

        // ============ QUICK ACTIONS ============
        function sendQuickAction(action) {
            const input = document.getElementById('userInput');
            input.value = action;
            sendMessage();
        }

        function regenerate() {
            if (state.lastPrompt) {
                generateImage(state.lastPrompt, '↻ Regenerated with a new variation:');
            }
        }

        // ============ GALLERY ============
        function addToGallery(url, prompt) {
            const item = { url, prompt, date: new Date().toISOString() };
            state.gallery.unshift(item);
            if (state.gallery.length > 50) state.gallery = state.gallery.slice(0, 50);
            localStorage.setItem('cai_gallery', JSON.stringify(state.gallery));
            renderGallery();
        }

        function renderGallery() {
            const grid = document.getElementById('galleryGrid');
            const empty = document.getElementById('galleryEmpty');
            if (state.gallery.length === 0) {
                grid.innerHTML = '';
                empty.classList.remove('hidden');
                return;
            }
            empty.classList.add('hidden');
            grid.innerHTML = state.gallery.map((item, i) => `
                <div class="gallery-img rounded-lg overflow-hidden aspect-square relative group" onclick="openPreview('${item.url}', '${escapeAttr(item.prompt)}')">
                    <img src="${item.url}" class="w-full h-full object-cover" loading="lazy">
                    <div class="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition flex items-end p-2">
                        <p class="text-[9px] text-white/80 line-clamp-2">${escapeHtml(item.prompt.substring(0, 60))}</p>
                    </div>
                </div>
            `).join('');
        }

        function clearGallery() {
            if (confirm('Clear all saved images?')) {
                state.gallery = [];
                localStorage.removeItem('cai_gallery');
                renderGallery();
                addAgentMessage('🗑️ Gallery cleared.');
            }
        }

        // ============ PREVIEW MODAL ============
        function openPreview(url, title) {
            document.getElementById('previewImg').src = url;
            document.getElementById('previewTitle').textContent = decodeURIComponent(title || 'Image Preview');
            document.getElementById('previewModal').classList.remove('hidden');
            document.getElementById('previewModal').classList.add('flex');
        }

        function closePreview() {
            document.getElementById('previewModal').classList.add('hidden');
            document.getElementById('previewModal').classList.remove('flex');
        }

        function downloadPreviewImage() {
            const url = document.getElementById('previewImg').src;
            downloadImage(url);
        }

        // ============ DOWNLOAD ============
        function downloadImage(url) {
            const a = document.createElement('a');
            a.href = url;
            a.download = `creativeai_${Date.now()}.png`;
            a.target = '_blank';
            document.body.appendChild(a);
            a.click();
            a.remove();
        }

        // ============ DEPLOY MODAL ============
        function showDeployModal() {
            document.getElementById('deployModal').classList.remove('hidden');
            document.getElementById('deployModal').classList.add('flex');
        }
        function closeDeployModal() {
            document.getElementById('deployModal').classList.add('hidden');
            document.getElementById('deployModal').classList.remove('flex');
        }

        // ============ CLEAR CHAT ============
        function clearChat() {
            const chat = document.getElementById('chatArea');
            chat.innerHTML = `
                <div class="flex justify-center animate-fade-in-up">
                    <div class="text-center max-w-lg py-8">
                        <div class="w-16 h-16 mx-auto mb-4 rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-500 flex items-center justify-center text-2xl shadow-lg shadow-violet-500/20">🎨</div>
                        <h2 class="text-xl font-bold gradient-text mb-2">Fresh Canvas</h2>
                        <p class="text-sm text-gray-400">Ready for a new creation. What shall we make?</p>
                    </div>
                </div>
            `;
            state.messages = [];
            state.lastPrompt = '';
            state.lastImageURL = '';
        }

        // ============ UTILS ============
        function escapeHtml(str) {
            const div = document.createElement('div');
            div.textContent = str;
            return div.innerHTML;
        }

        function escapeAttr(str) {
            return (str || '').replace(/'/g, "\\'").replace(/"/g, '\\"').replace(/\n/g, ' ');
        }

        function formatMarkdown(text) {
            return text
                .replace(/## (.*?)(\n|$)/g, '<h3 class="text-white font-bold text-base mb-2">$1</h3>')
                .replace(/\*\*(.*?)\*\*/g, '<strong class="text-white font-semibold">$1</strong>')
                .replace(/\*(.*?)\*/g, '<em>$1</em>')
                .replace(/`(.*?)`/g, '<code class="px-1.5 py-0.5 bg-white/10 rounded text-violet-300 text-xs">$1</code>')
                .replace(/^• (.*?)$/gm, '<div class="flex gap-2 items-start my-0.5"><span class="text-violet-400 mt-0.5">•</span><span>$1</span></div>')
                .replace(/\n\n/g, '<br><br>')
                .replace(/\n/g, '<br>');
        }

        // ============ KEYBOARD SHORTCUTS ============
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                closePreview();
                closeDeployModal();
            }
            // Ctrl/Cmd + K focuses input
            if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                e.preventDefault();
                document.getElementById('userInput').focus();
            }
        });

        // ============ START ============
        init();
    </script>
</body>
</html>
